#include <stdio.h> 
#include <math.h> 

int main()
{
int x,y,z,b;
printf(" x=");
scanf("%d",&x);
printf(" y=");
scanf("%d",&y);
printf("z=");
scanf("%d",&z);

b=1+cos(y-2)/(pow(x,4))/2+(sin (z)*sin (z));

printf("\n значення функці %d ",b);
       return 0;
}